<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.test.goods.dao.UserMapper">

    <select id="isLogin" parameterType="user" resultType="int">
        select count(*) from user where username = #{username} and email = #{email}
    </select>

    <insert id="add" parameterType="user">
        insert into user values (null, #{username} , #{password}, null, null, null, null, null)
    </insert>
    
    <update id="updateUserImage" parameterType="user" >
        update user set images = #{images} where email = #{email}
    </update>

</mapper>